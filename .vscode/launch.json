{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Build + Debug",
      "type": "hl",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "haxe: active configuration"
    }
  ]
}
